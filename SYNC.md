# 記帳雲端同步 — 設定步驟

做完之後，每記一筆帳都會自動存到你自己的 Google 試算表。換手機、重灌、誤刪 App 都讀得回來。

**這件事只做一次，而且只有你要做。** 同行的朋友什麼都不用設定——開網站就自動同步了。

建議在**電腦**上做，手機操作 Apps Script 很痛苦。約 10 分鐘。

---

## 1. 建一張試算表

到 <https://sheets.new> 開一張新的空白試算表，名字取「首爾記帳」之類的。

不用自己建欄位，程式第一次寫入時會自動開好一張叫 `expenses` 的工作表。

## 2. 打開 Apps Script

在這張試算表裡：**擴充功能 → Apps Script**。

會開一個新分頁，裡面有一個 `Code.gs`，內容大概是 `function myFunction() {}`。

## 3. 貼上程式碼

把 `Code.gs` 裡原本的內容**全部刪掉**，換成這個 repo 裡 `apps-script.gs` 的完整內容。

（GitHub 上打開 `apps-script.gs`，右上角有複製按鈕。）

貼完按存檔（磁碟片圖示，或 Ctrl+S / Cmd+S）。

## 4. 部署成網頁 App

右上角**部署 → 新增部署作業**。

- 左邊齒輪「選取類型」→ 選 **網頁應用程式**
- 「說明」隨便填，例如 `v1`
- 「執行身分」→ **我**（你自己）
- 「具有存取權的使用者」→ **所有人**  ← 這個一定要選對，不然手機連不上
- 按**部署**

## 5. 授權

第一次部署會跳出授權流程：

1. 「需要授權」→ 按**授權存取權**
2. 選你的 Google 帳號
3. 出現「Google 尚未驗證這個應用程式」的警告 → 點左下**進階** → **前往「專案名稱」(不安全)**
4. 按**允許**

這個警告是因為這支程式是你自己寫的、沒有送 Google 審核，不是真的有問題。它只會動你這張試算表。

## 6. 複製網址

部署完成後畫面上會有一個**網頁應用程式網址**，長這樣：

```
https://script.google.com/macros/s/AKfycb.................../exec
```

按旁邊的複製。**結尾一定要是 `/exec`**，如果是 `/dev` 就是複製錯了。

## 7. 把網址寫進 `index.html`

打開 `index.html`，找到這一行（在 `<script>` 裡，搜 `SYNC_URL` 就找得到）：

```js
const SYNC_URL="";
```

把網址填進去：

```js
const SYNC_URL="https://script.google.com/macros/s/AKfycb.................../exec";
```

存檔，改一下 `sw.js` 的 `VERSION` 尾碼數字，push 上去。

**這樣就完成了。** 之後任何人開這個網站都會自動同步，不用貼網址、不用設定任何東西。

> 留空的話 App 會退回舊模式：使用者要自己在 CLOUD SYNC 裡貼網址。功能一樣，只是多一步。

## 8. 朋友那邊要做什麼

**什麼都不用做。** 開網站、加到主畫面就好。

App 第一次開的時候會自動配一組六位代號（例如 `hwg7zz`），顯示在記帳頁最底下的 CLOUD SYNC 區塊。他的帳就記在那組代號底下，跟你的分開存，不會互相蓋掉。

想改成好記的名字就按「改代號」。

---

## 代號很重要

**代號就是找回資料的鑰匙。** 換手機的時候，在新手機上按「改代號」填回舊代號，紀錄就會自動接回來。

所以：

- **把 CLOUD SYNC 裡顯示的那組代號抄下來**（截圖也行）
- **同一個代號不要在兩支手機上同時用**，兩台會互相蓋掉

換代號時 App 會先去看雲端有沒有那組代號的資料：

- 雲端是空的 → 把這台的紀錄推上去
- 雲端有、這台是空的 → 直接接回來
- 兩邊都有 → 跳出詢問，預設**保住雲端那份**（按確定用雲端取代這台；按取消兩邊都不動，你可以改按「立即上傳」反方向處理）

## 平常怎麼運作

- 記一筆、改一筆、刪一筆 → 1.5 秒後自動上傳
- 沒網路 → 照記，狀態列顯示「有 N 筆還沒上傳」，連上網會自動補推
- 開 App 時 → 如果這支手機本來就有紀錄，**不會**被雲端蓋掉；只有本機是空的（重灌、換手機）才會自動接回來
- 兩邊筆數不一樣時狀態列會提醒，你自己決定要按「立即上傳」還是「從雲端讀回」

## 想確認資料真的存進去了

直接打開那張 Google 試算表，`expenses` 那張工作表就是全部紀錄，一列一筆。你可以直接看、篩選、匯出成 Excel。

**不要手動改試算表的內容**——下次手機上傳時會把你那個代號的列整批重寫，手改的東西會被蓋掉。要改就在 App 裡改。

## 安全性

**網址寫在 `index.html` 裡，而這個 repo 是公開的，所以這條網址等於公開的。** 任何人只要找到這個網站、按檢視原始碼，理論上就能讀寫這張試算表上的記帳資料。

實際的風險範圍：

- 對方要先知道 `dagutao86.github.io/2026_Seoul/` 這個網址存在
- 拿到的權限**只有這一張試算表**——Apps Script 綁在它上面，碰不到 Google 帳號的其他東西
- 最壞情況是旅遊記帳被看到或被亂寫，不涉及金錢或帳號

覺得不能接受的話有兩條路：把 `SYNC_URL` 改回空字串（退回讓使用者自己貼網址的模式），或把 repo 設成私人——但 GitHub Pages 從私人 repo 發布要付費方案。

真的想換掉網址：回 Apps Script → 部署 → 管理部署作業 → 停用舊的、新增一個新部署，拿到新網址後**要記得回來改 `index.html` 的 `SYNC_URL`**，舊網址會立刻失效。

## 不想用了

記帳頁 **改代號 → 關閉同步**。只是不再上傳，手機上跟試算表上的紀錄都不會被刪掉，按鈕會變成「開啟同步」，隨時可以開回來。
