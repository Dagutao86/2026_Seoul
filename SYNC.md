# 記帳雲端同步 — 設定步驟

> **已經設定過的人要更新**：程式碼換成共用帳本版了，要重貼一次。
> 到 Apps Script 把 `Code.gs` 全部換成新的 `apps-script.gs`，存檔，然後
> **部署 → 管理部署作業 → 鉛筆圖示 → 版本選「新版本」→ 部署**。
> **千萬不要按「新增部署作業」**——那會給你一條新網址，`index.html` 裡的 `SYNC_URL` 就失效了。
> 舊的 `expenses` 工作表不會被動到，留著當歷史備份；新資料寫在 `ledger` 和 `members` 兩張新的工作表。


做完之後，每記一筆帳都會自動存到你自己的 Google 試算表。換手機、重灌、誤刪 App 都讀得回來。

**這件事只做一次，而且只有你要做。** 同行的朋友什麼都不用設定——開網站就自動同步了。

建議在**電腦**上做，手機操作 Apps Script 很痛苦。約 10 分鐘。

---

## 1. 建一張試算表

到 <https://sheets.new> 開一張新的空白試算表，名字取「首爾記帳」之類的。

不用自己建欄位，程式第一次寫入時會自動開好 `ledger` 和 `members` 兩張工作表。

## 2. 打開 Apps Script

在這張試算表裡：**擴充功能 → Apps Script**。

會開一個新分頁，裡面有一個 `Code.gs`，內容大概是 `function myFunction() {}`。

## 3. 貼上程式碼

把 `Code.gs` 裡原本的內容**全部刪掉**，換成這個 repo 裡 `apps-script.gs` 的完整內容。

（GitHub 上打開 `apps-script.gs`，右上角有複製按鈕。）

貼完按存檔（磁碟片圖示，或 Ctrl+S / Cmd+S）。

## 4. 部署成網頁 App

右上角**部署 → 新增部署作業**。

- 左邊齒輪「選取類型」→ 選 **網頁應用程式**
- 「說明」隨便填，例如 `v1`
- 「執行身分」→ **我**（你自己）
- 「具有存取權的使用者」→ **所有人**  ← 這個一定要選對，不然手機連不上
- 按**部署**

## 5. 授權

第一次部署會跳出授權流程：

1. 「需要授權」→ 按**授權存取權**
2. 選你的 Google 帳號
3. 出現「Google 尚未驗證這個應用程式」的警告 → 點左下**進階** → **前往「專案名稱」(不安全)**
4. 按**允許**

這個警告是因為這支程式是你自己寫的、沒有送 Google 審核，不是真的有問題。它只會動你這張試算表。

## 6. 複製網址

部署完成後畫面上會有一個**網頁應用程式網址**，長這樣：

```
https://script.google.com/macros/s/AKfycb.................../exec
```

按旁邊的複製。**結尾一定要是 `/exec`**，如果是 `/dev` 就是複製錯了。

## 7. 把網址寫進 `index.html`

打開 `index.html`，找到這一行（搜 `SYNC_URL` 就找得到）：

```js
const SYNC_URL="";
```

把網址填進去，存檔，改一下 `sw.js` 的 `VERSION` 尾碼數字，push 上去。

**這樣就完成了。** 之後任何人開這個網站都會自動同步，不用貼網址、不用設定任何東西。

> 留空的話 App 會退回舊模式：使用者要自己在 CLOUD SYNC 裡貼網址。

## 8. 第一次用：加成員

記帳頁上方有一條「我是 —」，右邊是**成員**按鈕。按開來把同行的人都加進去（包含你自己）。

第一個加進去的人會自動被設成「我是誰」。之後點名字前面的方塊就能改。

**朋友那邊什麼都不用做**：開網站、加到主畫面，成員清單會自己同步過去，他只要點一下自己的名字就好。

---

## 抓匯率需要「連線至外部服務」權限

抓匯率用的是 `UrlFetchApp`，需要 `script.external_request` 權限。當初授權時程式碼還沒這段，Apps Script 不會自動重新要求，會一直回「你沒有呼叫 UrlFetchApp.fetch 的權限」。

解法是在資訊清單裡明確宣告權限，逼它重新授權：

1. Apps Script 編輯器左邊 **專案設定**（齒輪）→ 勾選**在編輯器中顯示「appsscript.json」資訊清單檔案**
2. 回到編輯器，左邊會多出 `appsscript.json`，把 repo 裡同名檔案的內容整個貼上去蓋掉，存檔
3. 函式下拉選單選 **`testRate`** → **執行** → 這次會跳出授權對話框
   → 選帳號 → 「Google 尚未驗證這個應用程式」點**進階 → 前往專案 (不安全)** → **允許**
4. 看**執行記錄**，會印出每個來源各自的 HTTP 狀態與回應
5. 最後 **部署 → 管理部署作業 → 鉛筆 → 新版本 → 部署**，讓線上的網頁 App 也帶著新權限

`appsscript.json` 裡的 `spreadsheets` 是記帳同步本來就要的，`script.external_request` 才是抓匯率新增的，兩個都要留著。

## 之後更新後端程式碼

`apps-script.gs` 改過的話，要手動同步到 Apps Script，網站才吃得到：

1. 打開試算表 → **擴充功能 → Apps Script**
2. 把 `apps-script.gs` 的內容整個貼上去蓋掉舊的，存檔
3. **部署 → 管理部署作業 → 右上角鉛筆 → 版本選「新版本」→ 部署**

第 3 步一定要用「管理部署作業 → 編輯 → 新版本」。按「新增部署作業」會產生**另一條網址**，而網址是寫死在 `index.html` 裡的，換掉就等於接到一張空試算表。

## 共用帳本怎麼運作

- **所有人共用同一本帳**，開這個網站的人看到的是同一份紀錄
- 每筆記帳要選**付款人**（誰先出的錢）和**分攤**（這筆算在誰頭上，預設全部人）
- 只有自己的花費就把分攤只勾自己
- 統計那排的**結算**頁會算出每個人的淨額，以及最少轉帳次數的還款方案
- 有人還錢之後按「已付款完成」，淨額就會沖銷掉。按錯可以在「已結清」那裡撤銷

## 兩支手機同時記帳會怎樣

不會互相蓋掉。每筆記錄帶一個唯一 ID 和時間戳，同步時按 ID 合併，同一筆被兩邊都改過就以**晚改的那次**為準。

刪除是留一個「已刪除」標記而不是真的刪掉，所以刪掉的東西不會被對方的手機復活。

**前提是每支手機的時間都要準。** 時間戳用的是各手機自己的時鐘，某支手機要是快了好幾小時，別人之後的修改和刪除都會因為「時間比較早」而被忽略，而且沒有任何跡象。App 現在每次同步都會跟伺服器對時，差超過 5 分鐘就會在 CLOUD SYNC 那裡跳紅字，看到就去把系統設定的日期時間改成自動判定。

## 結算為什麼用每日匯率

刷卡是**當天**結匯的。同樣一筆 ₩100,000，Day 1 刷跟 Day 5 刷，從戶頭扣掉的台幣不一樣。全部用同一個匯率結算的話，匯率波動就會變成某個人的損失。

所以結算頁把每一筆按**消費當天**的匯率換算成台幣再結算：

- 匯率湊齊時，淨額與轉帳金額都用**台幣**顯示，按「已付款完成」記下來的也是台幣
- 缺任何一天就退回**韓元**結算，數字仍然正確，只是沒有把波動算進去
- 本來就用台幣輸入的（機票、接駁那種）直接用實付金額，不需要匯率也不受影響

結算頁最下面的「每日匯率」按**自動抓匯率**就會補齊，也可以自己填。填一次會同步給所有人。

**匯率怎麼來的**：Visa 官方的匯率查詢端點擋伺服器對伺服器的請求（`usa.visa.com`／`visa.com.tw`／`visa.com.sg` 三個網域都回 HTTP 403，內容是 Cloudflare 機器人防護頁），所以改抓**市場匯率再加 0.6%** 來逼近刷卡實際結匯的匯率。加價方向是「每 1 韓元要付更多台幣」，也就是對持卡人略差，跟真實刷卡一致。

- 來源是免金鑰、有歷史資料的 `currency-api`（jsDelivr CDN 供應，不擋伺服器），三個網址依序備援
- 想用**實際的卡片匯率**就自己填，手動填的**不會被自動抓蓋掉**，也不會再被加 0.6%
- 加價幅度寫在 `apps-script.gs` 的 `MARKET_MARKUP`，要改就改那一行
- **未來的日期抓不到**，要等那天過了才有資料
- 合理範圍檢查（0.005–0.1）擋掉離譜的數字，不會被存進去

要單獨測試抓取，在 Apps Script 編輯器選 `testRate` 執行，執行紀錄會印出每個來源的結果。

## 平常怎麼運作

- 記一筆、改一筆、刪一筆、加成員 → 1.5 秒後自動同步
- 一次來回同時上傳與下載：本機整包送上去，伺服器合併之後把結果回傳
- 沒網路 → 照記，狀態列顯示「有變動還沒上傳」，連上網會自動補推
- 開 App 時會同步一次，所以換手機、重灌都會自己接回來——回來之後只要重新點一次「我是誰」

## 想確認資料真的存進去了

直接打開那張 Google 試算表：

- `ledger` — 全部記帳，一列一筆。`類型` 欄 `exp` 是消費、`pay` 是還款；`刪除` 欄是 `1` 的是已刪除的墓碑。還款列的 `分攤人` 欄若是 `tw`，代表那筆記的是台幣
- `members` — 成員清單
- `rates` — 每日匯率（1 韓元換多少台幣），`來源` 欄是 `市場`（已含 0.6% 加價）或 `手動`
- `expenses` — 舊版（個人帳本時代）的資料，留著當歷史備份，程式不會再動它

**不要手動改試算表的內容**——下次手機同步時會整批重寫，手改的東西會被蓋掉。要改就在 App 裡改。

## 安全性

**網址寫在 `index.html` 裡，而這個 repo 是公開的，所以這條網址等於公開的。** 任何人只要找到這個網站、按檢視原始碼，理論上就能讀寫這張試算表上的記帳資料。

實際的風險範圍：

- 對方要先知道 `dagutao86.github.io/2026_Seoul/` 這個網址存在
- 拿到的權限**只有這一張試算表**——Apps Script 綁在它上面，碰不到 Google 帳號的其他東西
- 最壞情況是旅遊記帳被看到或被亂寫，不涉及金錢或帳號

覺得不能接受的話，唯一有效的做法是把 `SYNC_URL` 改回空字串，退回讓使用者自己貼網址的模式。

**把 repo 設成私人沒有用，不要為這件事付錢。** GitHub Pro（約 US$4/月）只是讓私人 repo 也能發布 Pages，發布出去的網站本身仍然是公開的——而 `index.html` 就是那個被公開送到瀏覽器的檔案，按檢視原始碼一樣看得到 `SYNC_URL`。要連網站都鎖起來得用 GitHub Enterprise Cloud 的 Pages 存取控制，而且就算鎖了，進得去的人照樣讀得到原始碼。

更根本的是：**寫在前端網頁裡的東西藏不住**，這是瀏覽器的運作方式。Firebase 的 API key 也一樣公開，所以它靠伺服器端規則擋而不是靠隱藏。

真的想換掉網址：回 Apps Script → 部署 → 管理部署作業 → 停用舊的、新增一個新部署，拿到新網址後**要記得回來改 `index.html` 的 `SYNC_URL`**，舊網址會立刻失效。

## 不想用了

記帳頁 **設定 → 關閉同步**。只是不再上傳，手機上跟試算表上的紀錄都不會被刪掉，按鈕會變成「開啟同步」，隨時可以開回來。
