# 首爾行程 App — 專案說明

單頁 PWA，記錄 2026/8/20–8/26 首爾行程（時間軸卡片、Naver Map 連結、航班登機證卡）。純靜態網站，沒有 build step，GitHub Pages 直接讀檔案就能跑。

## 檔案結構

- `index.html` — 主體，所有行程內容、樣式、邏輯都在這一個檔案裡（GitHub Pages 會自動把這個當首頁，網址不用加檔名）
- `manifest.json` — PWA 設定（icon、名稱、啟動頁）
- `sw.js` — service worker，離線快取用
- `icon-192.png` / `icon-512.png` / `icon-maskable-512.png` / `apple-touch-icon.png` — App icon
- `fonts/ChenYuluoyan-2.0-Thin.woff2` — 標題用自訂字體（辰宇落雁體 2.0 Thin）

## 修改行程內容

所有行程資料都在 `index.html` 裡 `<script>` 開頭的兩個變數，改文字內容只需要動這兩個，不用碰 CSS 或版面邏輯：

- **`DAYS` 陣列** — 每天一個物件，`events` 是當天的時間軸項目，格式：
  `{ when: "時間", h: "標題", p: "說明文字", spots: ["韓文地點關鍵字"], key: true/false }`
  - `key: true` 會讓這個項目變成強調樣式（紅點、紅色左邊框），只用在真正重要的預約或航班，不要濫用
  - `spots` 陣列放**韓文**關鍵字，會自動生成 Naver Map 搜尋連結（中文地名 Naver 常搜不準）
  - `memo` 是每天結尾的提醒框，可有可無
- **`FLIGHTS` 物件** — 去回程航班資訊，對應 `out` 和 `in` 兩個 key，包含航班號、起降時間、航廈、PNR

## 重要規則：改動後一定要做的事

1. **每次修改 `index.html` 的行程內容後，`sw.js` 開頭的 `VERSION` 常數要加一**（例如 `seoul-v1` → `seoul-v2`）。這個 app 有離線快取，版本沒更新的話手機上會卡在舊版看不到新行程 —這是最容易漏掉但最重要的一步，每次改動前後都檢查一次。
2. 改完直接 commit 並 push 到 GitHub Pages 實際發布的分支（這是個人旅行小工具，不需要開 PR 走審核，確認改動內容後直接推上去）。
3. commit message 用簡短中文描述改了什麼，例如「Day4 改回聖水洞續攤」「更新航班時間」。
4. push 完可以順口提醒一句：手機上的 app 要離開再重新整理（或斷網重連）才會抓到新版本。

## 語言與風格

- 所有文字內容（行程說明、備忘、commit message）用繁體中文
- 地點的 `spots` 關鍵字一律用韓文，不要放中文或英文
- 設計語彙已經定調：米色／白色為底的韓紙（한지）質感，青／赤／黃三色只作重點強調用（不當背景大面積使用），登機證造型的航班卡。除非我明確要求，不要更動配色或版面結構，只改內容
- 字體：標題（首頁大標、每日標題、時間軸卡片標題）用自訂字體「辰宇落雁體 2.0 Thin」，檔案在 `fonts/ChenYuluoyan-2.0-Thin.woff2`，透過 `--f-title` 變數套用；內文說明文字維持霞鶩文楷 TC（`--f-cjk`），數字/英文標籤維持 Space Mono（`--f-mono`）。這是刻意的標題／內文字體搭配，不要把 `--f-title` 套到內文段落，會犧牲可讀性
- 有任何時間、地點、金額這類事實資訊要異動，優先確認我提供的原始資料（截圖、訂位確認信），不要自己推測填入
